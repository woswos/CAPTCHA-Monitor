from pathlib import Path
import os
import logging
from captchamonitor.utils.queue import Queue
import sys


def compose(args):
    """
    Automatically add new jobs base on the job history
    """
    logger = logging.getLogger(__name__)


    sys.exit()
